{
  "name": "petdogs-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Testes end-to-end independentes do frontend PetDogs",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
